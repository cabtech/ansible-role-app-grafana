---
# --------------------------------

- name: "ReloadGrafana"
  ansible.builtin.systemd:
    name: "{{ grafana_svc_name }}.service"
    state: reloaded
  become: true

- name: "RestartGrafana"
  ansible.builtin.systemd:
    name: "{{ grafana_svc_name }}.service"
    state: restarted
  become: true

# --------------------------------
...
